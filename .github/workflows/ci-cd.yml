name: CI/CD

on:
  push:
    branches:
      - main

  pull_request:
    branches:
      - main

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: ram2005024/baadfaad

jobs:

  test:
    name: Test
    runs-on: ubuntu-latest

    permissions:
      contents: read

    steps:

      - name: Checkout
        uses: actions/checkout@v6

      - name: Install uv
        uses: astral-sh/setup-uv@v7
        with:
          python-version: "3.12"

      - name: Install dependencies
        run: uv sync --dev

      - name: Django check
        env:
          DJANGO_SETTINGS_MODULE: config.settings.development
          SECRET_KEY: ci-test-secret
          DEBUG: "True"
          DB_NAME: test
          DB_USER: test
          DB_PASSWORD: test
          DB_HOST: localhost
          DB_PORT: "5432"
          REDIS_URL: redis://localhost:6379/0
          CELERY_BROKER_URL: redis://localhost:6379/1
          CELERY_RESULT_BACKEND: redis://localhost:6379/2
          ACCESS_TOKEN_LIFETIME_MINUTES: "30"
          REFRESH_TOKEN_LIFETIME_DAYS: "7"
          EMAIL_HOST: localhost
          EMAIL_PORT: "587"
          EMAIL_HOST_USER: ""
          EMAIL_HOST_PASSWORD: ""
          EMAIL_USE_TLS: "False"
          STORAGE_BACKEND: local
        run: uv run python manage.py check

      - name: Run tests
        env:
          DJANGO_SETTINGS_MODULE: config.settings.development
          SECRET_KEY: ci-test-secret
          DEBUG: "True"
          DB_NAME: test
          DB_USER: test
          DB_PASSWORD: test
          DB_HOST: localhost
          DB_PORT: "5432"
          REDIS_URL: redis://localhost:6379/0
          CELERY_BROKER_URL: redis://localhost:6379/1
          CELERY_RESULT_BACKEND: redis://localhost:6379/2
          ACCESS_TOKEN_LIFETIME_MINUTES: "30"
          REFRESH_TOKEN_LIFETIME_DAYS: "7"
          EMAIL_HOST: localhost
          EMAIL_PORT: "587"
          EMAIL_HOST_USER: ""
          EMAIL_HOST_PASSWORD: ""
          EMAIL_USE_TLS: "False"
          STORAGE_BACKEND: local
        run: uv run pytest


  build:
    name: Build and Push
    needs: test
    if: github.event_name == 'push'

    runs-on: ubuntu-latest

    permissions:
      contents: read
      packages: write

    steps:

      - name: Checkout
        uses: actions/checkout@v6

      - name: Login to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push Docker image
        uses: docker/build-push-action@v6
        with:
          context: .
          push: true
          tags: |
            ghcr.io/ram2005024/baadfaad:latest
            ghcr.io/ram2005024/baadfaad:${{ github.sha }}


  deploy:
    name: Deploy to EC2
    needs: build
    runs-on: ubuntu-latest

    steps:

      - name: Deploy
        uses: appleboy/ssh-action@v1.2.5
        with:
          host: ${{ secrets.EC2_HOST }}
          username: ${{ secrets.EC2_USER }}
          key: ${{ secrets.EC2_SSH_KEY }}
          script: |
            set -e

            cd ~/baadfaad

            echo "Pulling latest image..."
            docker compose pull

            echo "Starting containers..."
            docker compose up -d

            echo "Cleaning unused images..."
            docker image prune -af

            echo "Deployment complete."

            docker compose ps
